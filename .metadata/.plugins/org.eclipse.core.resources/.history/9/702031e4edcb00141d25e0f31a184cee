package org.emftrace.akm.ui.helpers;

import org.eclipse.swt.graphics.Color;
import org.eclipse.swt.widgets.Display;

public enum CapabilityTypeColorCoding {

	DevelopmentCapability("DevelopmentCapability", new Color(Display.getCurrent(), 255, 0, 0));

	private final Color mColor;

	CapabilityTypeColorCoding(final Color pColor) {

		mColor = pColor;
	}
	
	public  
}
